// Declaración de la variable my_image
image my_image = image("path/to/image.png");
// Definimos el filtro a utilizar
filter grayscale = filter("grayscale");
// Aplicamos el filtro
my_image.apply(grayscale);
// Redimensión de la imagen
my_image.resize(width=350, height=520);
// Guardamos la imagen
my_image.save()